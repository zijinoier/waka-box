name: Test waka
on:
  push:
    branches:
    - master
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Update gist
        uses: matchai/waka-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: 8d6011652319069f7449b66bcaa59a19f0c9a0d2
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
